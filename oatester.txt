    def search_paper(self, class_no): 
        subject_id = mysql.search("select subject_id from rxt_classes where class_no = %s"%class_no)
        grade_id = mysql.search("select grade_id from rxt_classes where class_no = %s"%class_no)
        url = "{}/{}".format(fist_url_teach, "exam/paper-index")
        headers = {"Authorization": self.get_token(), 'Content-Type': 'application/json'}
        params = {
            "subject_id": subject_id,
            "grade_id": grade_id
        }
        print(type(subject_id), type(grade_id))
        response = requests.get(url, headers=headers, params=params)
        bodyDict = response.json()
        return bodyDict